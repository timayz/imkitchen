# after create-story/story-context
`make check` must succeed, always run `make check` at the end to make sure it succeed, do not ignore test.
git checkout new, add -A, commit, push.
conventional commits multi-paragraph body:
  - story: 'docs/stories/xxx.md'
  - target audiences: end users that only use WEB UI
    - do not mention any abort claude code
create PR: 
  - title: same as commit
  - body: 
    - story tasks / subtasks
    - closed that issue xxx
    - do not mention any abort claude code
  - target: alpha

git checkout new,add -A,commit,push,create PR witch closed github issue 141 on merged

# init milestones / issues from epic
can you create all epics as milestone with associeted stories as issue ? epic title format
  "v0.{epic_num}", issue title format "{story_title}" with label story.

in folder fixtures, create recipe json file structured like "example-recipe.json" from
  [link]

I want to add multi language support. Can you replace all texts in templates/index.html, like 'My text' by '{{ "My test"|t }}' ?
then I want you to add them to fr.json like { "My test": "Mon test", "My test 2": "Mon test 2" }. Do not add duplicate transalation.
 

SELECT * FROM table WHERE id IN (SELECT id FROM table ORDER BY RANDOM() LIMIT x)
