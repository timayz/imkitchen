'docs/stories/story-0.1.md' create git branch, commit (conventional commits), push, create PR title format "feat: [story title]" in lowe case witch include tasks (task_title) checklist without subtasks (remove # from AC num) that closed issue id (seach with title filter by story title)

git commit (conventional commits), push
