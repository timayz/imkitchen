# after create-story/story-context
git checkout new, conventional commits, push.
create PR: 
  - title: start with "feat: impl",story title,no story num, in lower case 
  - body: 
    - include tasks (task_title) checklist without subtasks (remove # from AC num) 
    - closed issue id (use story title to search)

# after develop/review
run and fix make check than git conventional commits, push

# when working on test
when doing tests, use unsafe_oneshot that act like run but in sync way for subscribe

# init milestones / issues from epic
can you create all epics as milestone with associeted stories as issue ? epic title format
  "v0.{epic_num}", issue title format "{story_title}" with label story.

# before create-story / develop / review
read 'docs/solution-architecture.md'

in folder fixtures, create recipe json file structured like "example-recipe.json" from
  [link]

# Code Quality Fixes Completed (2025-10-17)
✅ Phase 1: All 4 critical fixes complete (~3.25h) - A-grade
  - Fix 1.1: Database transactions + idempotency
  - Fix 1.2: Silent error handling fixed
  - Fix 1.3: RotationCycleReset event emission
  - Fix 1.4: Race condition protection with RAII locks

✅ Phase 2: All 3 major fixes complete (~2h) - A+ grade
  - Fix 2.1: Validation + overflow protection
  - Fix 2.2: Eliminated unwrap() calls with logging
  - Fix 2.3: Rotation state in meal_replaced handler

All 47 tests passing. Excellent code quality, production-ready.
Next: Phase 3 (Optional: more tests) or ship to production.
