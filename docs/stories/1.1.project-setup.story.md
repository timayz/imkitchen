# Story 1.1: Project Setup and Development Environment

## Status
Draft

## Story
**As a** developer,
**I want** a fully configured Rust workspace with all dependencies and development tools,
**so that** I can efficiently develop and test the imkitchen application.

## Acceptance Criteria

**User Responsibilities:**
- None - This is a pure developer setup task

**Developer Agent Responsibilities:**
1. Rust workspace is configured with separate crates for core business logic, web server, and shared types
2. All specified dependencies are included: Axum 0.8+, Askama 0.14+, SQLx 0.8+, Tracing 0.1+, Evento 1.1+, twinspark-js, rust-i18n, config 0.15+
3. Development database (SQLite3) is initialized with schema migration system
4. Basic health check endpoint returns 200 OK with application status
5. Development server runs locally with hot reload capability
6. Structured logging is configured and operational across all modules
7. Configuration system loads from environment variables and config files
8. Basic error handling and middleware are implemented

## Tasks / Subtasks

- [ ] Initialize Rust workspace and crate structure (AC: 1)
  - [ ] Create workspace `Cargo.toml` in project root
  - [ ] Create `imkitchen-core` crate for business logic
  - [ ] Create `imkitchen-web` crate for web server
  - [ ] Create `imkitchen-shared` crate for shared types
  - [ ] Configure workspace dependencies and features

- [ ] Add required dependencies to appropriate crates (AC: 2)
  - [ ] Add Axum 0.8+ to `imkitchen-web` crate
  - [ ] Add Askama 0.14+ for templating
  - [ ] Add SQLx 0.8+ for database operations
  - [ ] Add Tracing 0.1+ for structured logging
  - [ ] Add Evento 1.1+ for event-driven architecture
  - [ ] Add twinspark-js for frontend enhancement
  - [ ] Add rust-i18n for internationalization
  - [ ] Add config 0.15+ for configuration management

- [ ] Initialize SQLite database with migration system (AC: 3)
  - [ ] Set up SQLx CLI for migrations
  - [ ] Create initial database schema migration
  - [ ] Configure SQLite connection in development
  - [ ] Create database initialization script

- [ ] Implement basic web server with health endpoint (AC: 4)
  - [ ] Create main server module in `imkitchen-web`
  - [ ] Implement `/health` endpoint returning 200 OK with JSON response
  - [ ] Configure basic Axum router setup
  - [ ] Add application status information to health response (version, database status, uptime)

- [ ] Configure development server with hot reload (AC: 5)
  - [ ] Set up `cargo-watch` for automatic recompilation
  - [ ] Configure development server startup script
  - [ ] Test hot reload functionality works correctly

- [ ] Implement structured logging system (AC: 6)
  - [ ] Configure tracing subscriber in main application
  - [ ] Set up log levels and format for development
  - [ ] Add structured logging to all modules
  - [ ] Configure log output to console and file

- [ ] Create configuration management system (AC: 7)
  - [ ] Define configuration structure using `config` crate
  - [ ] Load configuration from environment variables
  - [ ] Support configuration files (TOML/YAML)
  - [ ] Create development configuration defaults

- [ ] Implement basic error handling and middleware (AC: 8)
  - [ ] Create custom error types using `thiserror`
  - [ ] Implement error middleware for web requests
  - [ ] Add request/response logging middleware
  - [ ] Configure CORS and security headers

- [ ] Create development utilities and scripts
  - [ ] Create `scripts/dev-setup.sh` for environment initialization
  - [ ] Create `scripts/reset-db.sh` for database reset
  - [ ] Create `scripts/health-check.sh` for endpoint testing
  - [ ] Add comprehensive error handling for health endpoint (503/500 responses)

## Dev Notes

### Previous Story Insights
No previous stories exist - this is the foundation story.

### Architecture Context
[Source: architecture/high-level-architecture.md#technical-summary]
- **Architecture Pattern**: Rust fullstack monolithic architecture deployed as single binary PWA
- **Web Framework**: Axum 0.8+ for HTTP server and routing
- **Template Engine**: Askama 0.14+ for server-side HTML rendering
- **Event System**: Evento 1.1+ for internal event-driven architecture
- **Database**: SQLite 3.40+ for embedded data persistence

[Source: architecture/high-level-architecture.md#repository-structure]
- **Structure**: Single Rust workspace monorepo with separate crates for modularity
- **Tool**: Cargo workspaces (native Rust solution)
- **Organization**: Functional separation through Rust crates while maintaining single binary output

### Data Models
[Source: architecture/data-models.md#user]
Core entities that will need database schema:
- **User**: uuid id, email, password_hash, name, family_size, dietary_restrictions, cooking_skill_level, timestamps
- **Recipe**: uuid id, title, description, timing, difficulty, servings, ingredients array, instructions array
- **MealPlan**: uuid id, user_id, week_start_date, meals array, status enum
- **ShoppingList**: uuid id, meal_plan_id, items array, sharing configuration

### API Specifications
[Source: architecture/api-specification.md#rest-api-specification]
Foundation endpoints to prepare for:
- `/api/auth/register` and `/api/auth/login` for user authentication
- `/api/meal-plans` for meal planning functionality
- `/api/recipes` for recipe management
- `/api/shopping-lists/{mealPlanId}` for shopping lists

### Tech Stack Requirements
[Source: architecture/tech-stack.md#technology-stack-table]
**Required Dependencies with Versions:**
- Rust 1.70+
- Axum 0.8+ (HTTP server, routing)
- Askama 0.14+ (server-side HTML templates)
- SQLx 0.8+ (async database, no compile-time query checking)
- Tracing 0.1+ (structured logging, observability)
- Evento 1.1+ (event-driven architecture)
- rust-i18n (internationalization)
- config 0.15+ (configuration management)
- twinspark-js (progressive client-side interactivity)

**Additional Dev Tools:**
- tokio-test + rstest for testing
- cargo-watch for hot reload
- thiserror for custom error types

### File Locations
Based on monorepo structure, create:
- `/Cargo.toml` - Workspace configuration
- `/crates/imkitchen-core/` - Business logic crate
- `/crates/imkitchen-web/` - Web server crate  
- `/crates/imkitchen-shared/` - Shared types crate
- `/migrations/` - SQLx database migrations
- `/config/` - Configuration files

### Technical Constraints
[Source: architecture/tech-stack.md#technology-stack-table]
- **Database**: SQLite for embedded deployment eliminating external dependencies
- **Performance**: Single binary deployment for cost-effectiveness
- **Security**: Custom authentication following OWASP guidelines
- **Architecture**: Event-driven patterns using Evento for decoupled services

### Testing Requirements
**Testing Framework Setup:**
- Unit tests in `tests` modules within each crate
- Integration tests in `/tests` directory  
- Use `tokio-test` for async testing
- Use `rstest` for parameterized tests

**Specific Test Scenarios Required:**
1. **Health Endpoint Tests:**
   - Test `/health` returns 200 OK with proper JSON structure
   - Test health response includes version, database status, uptime fields
   - Test health endpoint responds within 100ms under normal conditions
   - Test health endpoint behavior when database is unavailable

2. **Configuration Loading Tests:**
   - Test loading from environment variables
   - Test loading from TOML/YAML configuration files  
   - Test configuration defaults when no config files present
   - Test configuration validation for required fields

3. **Database Initialization Tests:**
   - Test SQLite database creation and migration execution
   - Test database connection establishment
   - Test migration rollback functionality (if implemented)

4. **Server Startup Tests:**
   - Test server starts and binds to configured port
   - Test server graceful shutdown
   - Test hot reload functionality works correctly

**Performance Requirements:**
- Health endpoint must respond within 100ms
- Server startup must complete within 5 seconds
- Database initialization must complete within 2 seconds

### Development Scripts and Error Handling
**Recommended Development Scripts to Create:**
- `scripts/dev-setup.sh` - Complete development environment initialization
- `scripts/reset-db.sh` - Reset database with fresh migrations
- `scripts/health-check.sh` - Test health endpoint during development

**Error Scenarios to Handle:**
1. **Health Endpoint Error Responses:**
   - Return 503 Service Unavailable if database connection fails
   - Return 500 Internal Server Error for configuration issues
   - Include error details in response for development environment only

2. **Database Connection Errors:**
   - Graceful startup failure with clear error message if database unavailable
   - Connection retry logic with exponential backoff
   - Migration failure handling with rollback capability

3. **Configuration Errors:**
   - Clear validation errors for missing required configuration
   - Helpful error messages pointing to configuration documentation
   - Default value warnings in development mode

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|---------|
| 2024-09-24 | 1.0 | Initial story creation | Scrum Master |
| 2024-09-24 | 1.1 | Enhanced testing specifications, performance benchmarks, error scenarios, and development scripts | Product Owner |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results
*Results from QA Agent review will be added here*