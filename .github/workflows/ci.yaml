name: CI

on:
  pull_request:
  push:
    branches:
      - main
    paths-ignore:
      - "**/Cargo.toml"
      - "Cargo.lock"

jobs:
  test:
    uses: timayz/.github/.github/workflows/rust-test.yml@feat/rust-test/test-matrix
    with:
      tests: '["-p user", "-p meal_planning", "-p notifications", "-p recipe", "-p shopping"]'
    #uses: timayz/.github/.github/workflows/rust-test.yml@v3
    #
